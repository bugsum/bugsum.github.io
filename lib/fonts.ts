import { Comfortaa, JetBrains_Mono, Space_Grotesk, Syncopate } from 'next/font/google';

export const spaceGrotesk = Space_Grotesk({
    subsets: ['latin'],
    weight: '400',
});

export const jetbrainsMono = JetBrains_Mono({
    weight: '600',
    subsets: ['latin'],
});

export const syncopate = Syncopate({
    weight: '700',
    subsets: ['latin'],
});

export const comfortaa = Comfortaa({
    weight: '400',
    subsets: ['latin'],
});
